import React from 'react';
import ClimateCropPredictor from '../components/ClimateCropPredictor';

/**
 * ClimatePredictor page
 *
 * Wraps the ClimateCropPredictor component and serves as the routed page
 * at /climate-predictor.
 */
export default function ClimatePredictor() {
  return <ClimateCropPredictor />;
}
